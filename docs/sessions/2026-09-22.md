# Session: 2026-09-22

## Goal
Turn Clock into a polished native desktop utility and refine resizing, settings, timer controls and compact presentation through user feedback.

## Progress
- Added window-fitted typography, optional date/time-zone/frame display, and searchable time zones with cities, countries, continents, abbreviations, offsets and favourites. Bundled offline GeoNames data with attribution.
- Added independent persistent clock windows, custom labels, day differences, countdown/stopwatch, rational fractional frame rates and drop-frame numbering.
- Added native menus, status item, global visibility shortcut, borderless/opacity/all-Spaces controls, login integration, signed app bundle and icon.
- Replaced dynamic Controls-menu rebuilding after an automated menu interaction stuck in tracking. User approved force-quitting that instance; subsequent launches quit normally.
- Refined native expanded toolbar, timer action buttons, fixed-size toggleable status line and window-attached duration sheet. Fixed text sizing after mode switches.
- Tab toggles clock-only presentation by adding/removing chrome around the current clock area. Resizing carries through in either direction. Re-enabling AppKit frame autosave initially restored the stale normal frame; saving the newly calculated frame first fixes it. User confirmed pass with Remember Window Frame enabled. Settings and duration sheets retain normal Tab navigation.
- Duration fields validate immediately with field-specific limits; invalid input disables Set Duration.
- Validation: `swift test` passed all 13 tests; `scripts/build-app.sh` produced a signed bundle; `git diff --check` passed. Tests cover clock/timer/borderless resizing, repeated toggles, actual AppKit frame autosaving, and restoration in a new controller. The autosave regression failed before the fix and passed afterward.
- Gracefully replaced the installed app and verified `/Applications/Clock.app/Contents/MacOS/Clock` running (PID 26074 after the latest fix). Installed and built executables match; installed signature verifies outside the sandbox (sandboxed codesign gave a misleading failure).
- Read-only login preflight found no Clock entry in the background-task registration output. No real login or notification-delivery acceptance was performed. Those remain open.
- User requested saving the working state and continuing acceptance checks. Created a local Git checkpoint of the current app, resources, tests and documentation; no remote was configured at that point.
- Published the tested source as the public `Xpycode/CountClockWise` repository and fast-forwarded the completed shipping work to `main`. Reviewed tracked history for credential-pattern candidates; no secret was identified.
- Corrected the public README build number and removed the remaining Help image-provider deprecation warning. All 25 tests pass; a fresh Developer ID-signed 1.0 (1002) build was produced and launched from the exact build artifact.
- Prepared `feature/count-clock-wise-registration` in App-Websites with the inactive feedback/tip roster entry. Website invariants pass and the branch is pushed; it has not been merged or deployed.
- Release audit confirmed the complete app-icon size set and valid Developer ID/Hardened Runtime signing. Gatekeeper still reports the build as unnotarized. No notary profile exists on this Mac; the credential-vault audit says the existing App Store Connect API key should be rotated. Sparkle remains inactive until its app-specific Ed25519 public key and HTTPS appcast are configured.

## Decisions
- Keep automatic text fitting; remove the redundant manual text-size setting.
- Use native AppKit toolbar/buttons/sheets and stable menu objects. Keep presentation chrome optional, with transient Tab focus mode for a compact display.
- Timecode remains a local display, not LTC/MTC synchronization. Public distribution and notarization were outside the original should-fix batch and remain explicit release gates.
- Keep the website roster entry inactive until deployment is explicitly approved. A public source repository is not a downloadable app release.
- Do not reuse the credential-vault API key silently; rotate it before creating the app's notarization profile.

## Next
- After explicit approval, create the README app-icon asset and representative screenshots.
- Rotate/store notarization credentials, configure and publish the signed Sparkle channel, activate the prepared website entry, then notarize and test the final downloadable artifact.
- Verify macOS 14/clean-user launch, launch at login and countdown notification delivery.

## Resume
- Public app repo: `https://github.com/Xpycode/CountClockWise`, branch `main`. App-Websites handoff: pushed branch `feature/count-clock-wise-registration`, not merged or deployed. Pull `main` on another Mac after Syncthing settles.
- Exact pickup: obtain visual-work approval, then add the README icon/screenshots. For distribution, rotate the flagged App Store Connect API key before storing a `CountClockWise` notary profile; create the app-specific Sparkle key/feed and run the real update/notarization/Gatekeeper acceptance chain.
- Deferred: 4 broader acceptance items; Inbox: 0. No half-done source edit. The exact fresh build is running from `build/Count Clock Wise.app`; the Applications copy was not replaced in this continuation.
- Model fit: deep capability + high reasoning for credential rotation, signed-update publication and release verification. Current setting is not reliably visible.

## Sync
- Count Clock Wise `main` is pushed to the new public origin. On another Mac, clone it or pull `main`; no app feature branch remains locally.
- App-Websites branch `feature/count-clock-wise-registration` is committed and pushed for handoff. It is intentionally unmerged and undeployed.
